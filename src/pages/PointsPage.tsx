import { PagePlaceholder } from '@/components/PagePlaceholder';

export function PointsPage() {
  return (
    <PagePlaceholder
      title="Point Manager"
      description="当前页面仅为路由空壳，尚未接入点位模型、列表、导入、转换或本地存储。"
    />
  );
}
